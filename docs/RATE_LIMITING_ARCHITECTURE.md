# 🏗️ Rate Limiting Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT REQUEST                           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      EXPRESS SERVER                              │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  General API Rate Limiter (100 req/15min)                  │ │
│  │  Applied to: /api/*                                        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                              │                                    │
│                              ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              ROUTE-SPECIFIC RATE LIMITERS                  │ │
│  │                                                             │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │ /api/auth (5 req/15min)                              │ │ │
│  │  │  • POST /signup                                      │ │ │
│  │  │  • POST /login                                       │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                                                             │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │ /api/auth/password (3 req/hour)                      │ │ │
│  │  │  • POST /forgot-password                             │ │ │
│  │  │  • POST /reset-password                              │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                                                             │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │ /api/ai (10 req/min)                                 │ │ │
│  │  │  • POST /smart-replies                               │ │ │
│  │  │  • POST /translate                                   │ │ │
│  │  │  • POST /chatbot                                     │ │ │
│  │  │  • POST /sentiment                                   │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                                                             │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │ /api/messages (30 req/min)                           │ │ │
│  │  │  • POST /send/:id                                    │ │ │
│  │  │  • POST /forward/:messageId                          │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                                                             │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │ /api/calls (5 req/min)                               │ │ │
│  │  │  • POST /initiate                                    │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      SOCKET.IO SERVER                            │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │         SOCKET EVENT RATE LIMITERS (Per User)              │ │
│  │                                                             │ │
│  │  Event              │ Limit  │ Window                      │ │
│  │  ──────────────────────────────────────────────────────   │ │
│  │  typing             │ 10     │ 10 seconds                  │ │
│  │  game:move          │ 30     │ 1 minute                    │ │
│  │  cursor:move        │ 100    │ 1 second                    │ │
│  │  random:message     │ 20     │ 1 minute                    │ │
│  │  game:invite        │ 5      │ 1 minute                    │ │
│  │  call:offer         │ 5      │ 1 minute                    │ │
│  │  room:join          │ 10     │ 1 minute                    │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## Request Flow with Rate Limiting

### HTTP Request Flow
```
Client Request
    │
    ▼
┌─────────────────────┐
│ General API Limiter │ ◄── 100 req/15min
│ (All /api/* routes) │
└─────────────────────┘
    │
    ▼
┌─────────────────────┐
│ Route-Specific      │ ◄── Stricter limits
│ Limiter             │     (5-30 req/min)
└─────────────────────┘
    │
    ▼
┌─────────────────────┐
│ Auth Middleware     │ ◄── JWT validation
│ (protectRoute)      │
└─────────────────────┘
    │
    ▼
┌─────────────────────┐
│ Controller          │ ◄── Business logic
│ (Handle request)    │
└─────────────────────┘
    │
    ▼
Response to Client
```

### Socket Event Flow
```
Socket Event
    │
    ▼
┌─────────────────────┐
│ Socket Rate Limiter │ ◄── Per-event limits
│ (checkLimit)        │     (5-100 req/window)
└─────────────────────┘
    │
    ├─── Limit OK ──────────────┐
    │                            ▼
    │                    ┌─────────────────┐
    │                    │ Process Event   │
    │                    └─────────────────┘
    │
    └─── Limit Exceeded ────────┐
                                 ▼
                         ┌─────────────────┐
                         │ Emit Error      │
                         │ (Silent drop)   │
                         └─────────────────┘
```

---

## Rate Limiter Storage

### HTTP Rate Limiters (In-Memory)
```
┌──────────────────────────────────────┐
│     Express Rate Limit Store         │
│                                      │
│  IP Address → Request Count          │
│  ─────────────────────────────────   │
│  192.168.1.1 → 3/5 (Auth)           │
│  192.168.1.2 → 8/10 (AI)            │
│  192.168.1.3 → 25/30 (Messages)     │
│                                      │
│  Auto-reset after window expires     │
└──────────────────────────────────────┘
```

### Socket Rate Limiters (Custom Map)
```
┌──────────────────────────────────────┐
│    Socket Rate Limiter Store         │
│                                      │
│  User ID → Event → Count             │
│  ─────────────────────────────────   │
│  user123 → typing → 5/10             │
│  user123 → game:move → 12/30         │
│  user456 → call:offer → 2/5          │
│                                      │
│  Cleanup every 5 minutes             │
└──────────────────────────────────────┘
```

---

## Multi-Layer Protection

```
┌─────────────────────────────────────────────────────────┐
│                    LAYER 1: GENERAL                      │
│              100 requests per 15 minutes                 │
│                  (All API routes)                        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                 LAYER 2: ROUTE-SPECIFIC                  │
│                                                          │
│  Auth: 5/15min  │  AI: 10/min  │  Messages: 30/min     │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  LAYER 3: AUTHENTICATION                 │
│                    JWT Validation                        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  LAYER 4: BUSINESS LOGIC                 │
│                  Controller Processing                   │
└─────────────────────────────────────────────────────────┘
```

---

## Attack Scenarios & Protection

### 1. Brute Force Login Attack
```
Attacker tries 1000 login attempts
    │
    ▼
Auth Rate Limiter (5/15min)
    │
    ├─── Attempt 1-5: Allowed
    │
    └─── Attempt 6+: BLOCKED ❌
         Response: 429 Too Many Requests
```

### 2. AI API Abuse
```
User spams AI chatbot
    │
    ▼
AI Rate Limiter (10/min)
    │
    ├─── Request 1-10: Allowed
    │
    └─── Request 11+: BLOCKED ❌
         Response: 429 Too Many Requests
```

### 3. Message Spam
```
User sends 100 messages/second
    │
    ▼
Message Rate Limiter (30/min)
    │
    ├─── Message 1-30: Allowed
    │
    └─── Message 31+: BLOCKED ❌
         Response: 429 Too Many Requests
```

### 4. Socket Event Flooding
```
User floods typing events
    │
    ▼
Socket Rate Limiter (10/10sec)
    │
    ├─── Event 1-10: Processed
    │
    └─── Event 11+: SILENTLY DROPPED ❌
         (No error emitted)
```

---

## Production Scaling

### Single Server (Current)
```
┌──────────────────┐
│   Express App    │
│                  │
│  ┌────────────┐  │
│  │ In-Memory  │  │
│  │   Store    │  │
│  └────────────┘  │
└──────────────────┘
```

### Multi-Server (Redis)
```
┌──────────────────┐     ┌──────────────────┐
│   Server 1       │     │   Server 2       │
│                  │     │                  │
│  ┌────────────┐  │     │  ┌────────────┐  │
│  │   Redis    │◄─┼─────┼─►│   Redis    │  │
│  │  Adapter   │  │     │  │  Adapter   │  │
│  └────────────┘  │     │  └────────────┘  │
└──────────────────┘     └──────────────────┘
         │                        │
         └────────┬───────────────┘
                  ▼
         ┌──────────────────┐
         │   Redis Server   │
         │  (Shared Store)  │
         └──────────────────┘
```

---

## Monitoring & Alerts

### Rate Limit Hit Tracking
```javascript
// Log when limits are hit
authLimiter.handler = (req, res) => {
  console.log(`⚠️  Rate limit hit: ${req.ip} on ${req.path}`);
  // Send to monitoring service (e.g., Datadog, New Relic)
  res.status(429).json({ message: 'Too many requests' });
};
```

### Metrics to Track
- **Total rate limit hits per endpoint**
- **Top offending IP addresses**
- **Peak request times**
- **False positive rate** (legitimate users blocked)

---

## Configuration Matrix

| Endpoint Type | Limit | Window | Strictness | Reason |
|--------------|-------|--------|------------|--------|
| Auth (Login) | 5 | 15 min | 🔴 High | Prevent brute force |
| Password Reset | 3 | 1 hour | 🔴 High | Prevent abuse |
| AI Endpoints | 10 | 1 min | 🟡 Medium | API cost control |
| Messages | 30 | 1 min | 🟡 Medium | Prevent spam |
| Calls | 5 | 1 min | 🟡 Medium | Prevent harassment |
| General API | 100 | 15 min | 🟢 Low | Baseline protection |
| Socket Events | Varies | Varies | 🟡 Medium | Prevent flooding |

---

## Best Practices Applied

✅ **Layered approach** - Multiple rate limiters for defense in depth  
✅ **Different limits** - Stricter for sensitive endpoints  
✅ **User-friendly messages** - Clear error responses  
✅ **Standard headers** - RateLimit-* headers for transparency  
✅ **Automatic cleanup** - Memory-efficient implementation  
✅ **Per-user tracking** - Fair resource allocation  
✅ **Silent drops** - For non-critical socket events  

---

## Future Enhancements

1. **Redis Integration** - For multi-server deployments
2. **Dynamic Limits** - Adjust based on user reputation
3. **Whitelist/Blacklist** - IP-based access control
4. **Rate Limit Dashboard** - Real-time monitoring UI
5. **Machine Learning** - Detect anomalous patterns
6. **Geographic Limits** - Different limits per region

---

**🎉 Your application is now protected with enterprise-grade rate limiting!**
